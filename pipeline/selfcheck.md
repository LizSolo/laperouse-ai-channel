# Самопроверка за 2026-09-13
Скрипт: pipeline/lint.py, код 0, отчёт в pipeline/lint.md

Режим отбора включён (T-15): по каждому посту только пять судейских правил —
T-01, X-02, X-09, X-11, X-18. Остальное закрывает скрипт (см. pipeline/lint.md).

## Пост 1. Cursor заблокирован в России после сделки со SpaceX
T-01: да — «читатель, использующий Cursor для кода на 1С или в целом, переходит на Claude Code/Codex, локальные модели или доступные из России платные альтернативы вместо обхода блокировки через VPN» (pipeline/funnel.md) — судейское: действие конкретное (сменить инструмент или подготовить запасной), читатель вне AI-ниши (владелец бизнеса/разработчик на аутсорсе, не сама ИИ-лаборатория)
X-02: да — «Если ваша команда писала код в Cursor, с 4 сентября сервис не работает из России совсем, не только по картам.» — судейское: первое предложение сразу про то, что меняется для читателя (сервис перестал работать), а не про то, кто и за сколько купил компанию
X-09: да — одна мысль: иностранный ИИ-инструмент могут заблокировать в любой момент, держите запасной вариант; фактура про SpaceX и OpenAI подчинена этой мысли, не тянет на отдельный пост
X-11: да — судейское: длины и конструкции предложений заметно разные (короткое с обращением на «вы», среднее с уточнением в скобках, перечисление через двоеточие, вопрос-совет в повелительном наклонении), трёх одинаковых подряд нет
X-18: да — список из 4 замен для 1С, лишнего пункта «для количества» нет, каждый инструмент отличается от других (браузерная IDE / плагин для EDT / официальный ассистент / прямая интеграция)

## Пост 2. GitHub простыми словами для тех, кто работает с агентами
T-01: да — «читатель, который поручает разработку ИИ-агенту, заводит репозиторий и просит агента коммитить после каждой правки вместо работы без точек отката» (pipeline/funnel.md) — судейское: действие однострочное и конкретное, читатель — нетехнический владелец бизнеса, а не разработчик инструментов ИИ
X-02: да — «Если код для вас теперь пишет ИИ-агент, у вас должен быть способ откатиться назад, когда он всё сломает.» — судейское: сразу о последствии для читателя (нужен способ откатиться), а не о том, что такое GitHub технически
X-09: да — одна мысль: держите код в репозитории ради отката, три причины и список шагов её раскрывают, а не уводят в сторону
X-11: да — судейское: предложения разной длины и структуры (короткое условное, объяснение через двоеточие, перечисление, вывод от первого лица, два императива подряд в последнем абзаце — разного типа: «заведите и попросите» / «сломайте и откатитесь»), подряд одинаковых трёх нет
X-18: да — список из 3 причин, все три разные по сути (защита от правок / автодеплой / требование облачных агентов), лишней третьей ради красоты нет

## Пост 3. Голосовое сообщение превращается в задачу
T-01: да — «читатель настраивает у себя приём голосовых поручений сотрудников в таблицу задач вместо ручного разбора войсов» (pipeline/funnel.md) — судейское: действие конкретное и однострочное, применимо к любому небольшому бизнесу с сотрудниками
X-02: да — «Голосовые поручения от сотрудников часто теряются в переписке, а их можно на автомате превращать в задачи со сроком и исполнителем.» — судейское: сразу о проблеме и решении для читателя, а не о том, что вышел новый сервис
X-09: да — одна мысль: голосовые поручения можно автоматически превращать в задачи; оговорка про оплату Gemini подчинена той же мысли (на чём это technически держится), не отдельный сюжет
X-11: да — судейское: разная длина и конструкция (описание цепочки через двоеточие, личная оценка с встроенным «а не», сухая оговорка про оплату, двойной императив в финале), трёх одинаковых подряд нет
X-18: не применимо — в посте нет перечисления из трёх однородных элементов в одном предложении

## Пост 4. Unity выпустила официальный плагin для Claude Code, Codex и Grok
T-01: да — «читатель из геймдева подключает агента напрямую к проекту через готовые скиллы вместо переноса кода вручную» (pipeline/funnel.md) — судейское: действие конкретное, хотя аудитория узкая (геймдев), что не является отказом по T-01 в режиме отбора
X-02: да — «Если ваша студия уже пускает ИИ-агентов писать код, для Unity 6 теперь есть отдельный официальный набор навыков под них.» — судейское: сразу про то, что меняется для читателя (появился набор навыков под уже используемых агентов), а не про релиз Unity как таковой
X-09: да — одна мысль: готовые скиллы снижают число ошибок агента в Unity-проекте; список скиллов и абзац про авторство их раскрывают, второго сюжета нет
X-11: да — судейское: предложения разной длины (короткое условное, среднее с перечислением через «и», развёрнутое с личной оценкой и двоеточием, финальный императив), подряд одинаковых трёх нет
X-18: да — список из 3 строк группирует больше 30 скиллов по смыслу, каждая строка про разные категории функций, лишней ради счёта нет

## Пост 5. Claude спроектировала печатную плату по одному промпту
T-01: да — «читатель с задачей разработки печатной платы пробует отдать первый черновик агенту вместо найма инженера с нуля» (pipeline/funnel.md) — судейское: действие конкретное при узкой аудитории (инженеры-электронщики, DIY), узость сама по себе не отказ по T-01
X-02: да — «Один инженер поручил Claude спроектировать печатную плату целиком по одному текстовому запросу, и она получилась рабочей.» — судейское: сразу о результате для похожей задачи читателя, а не о характеристиках модели
X-09: да — одна мысль: агент уже справляется с проектированием платы без инженера рядом; цифры по стоимости и оговорка про узость аудитории ей подчинены
X-11: да — судейское: предложения разной длины и конструкции (перечисление компонентов через запятые, сложное предложение с «а через», оценка с встроенным «а не», финал с двумя разными по типу советами), подряд одинаковых трёх нет
X-18: не применимо — перечислений из трёх и более однородных элементов в одном предложении в посте нет (список компонентов платы — не однородные элементы одного тезиса, а технические детали одного запроса)

## Пост 6. Workflows в Yandex AI Studio
T-01: да — «читатель с повторяющейся задачей (обработка видео, разбор тональности обращений, сбор и оформление данных) собирает пайплайн визуально вместо найма разработчика под одноразовый скрипт» (pipeline/funnel.md) — судейское: действие конкретное и однострочное
X-02: да — «Если у вас в компании есть повторяющийся процесс вроде разбора обращений или обработки видео, теперь его можно собрать без разработчика.» — судейское: сразу о том, что меняется для читателя, а не о том, что Яндекс выпустил инструмент
X-09: да — одна мысль: повторяющийся процесс можно собрать без разработчика визуально; примеры сценариев и личная оценка её раскрывают, второго сюжета нет
X-11: да — судейское: предложения разной длины и конструкции (описание инструмента, перечисление триггеров через запятую и «а дальше», личная оценка с «а не», финальный совет-условие), подряд одинаковых трёх нет
X-18: да — список из 3 сценариев, каждый про разную задачу (текст / видео / тональность), лишнего пункта ради количества нет
