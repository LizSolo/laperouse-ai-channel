# Самопроверка за 2026-09-08
Скрипт: pipeline/lint.py, код 0, отчёт в pipeline/lint.md

## Пост 1. Amvera Polide Agent
T-01: да — «читатель прикинет, может ли собрать лендинг или Telegram-бота через Polide Agent вместо найма разработчика» — судейское: действие названо глаголом (собрать/сравнить), владелец бизнеса вне AI-ниши может выполнить его сам, не нанимая разработчика
X-02: да — «Если для лендинга или Telegram-бота вам сейчас нужен разработчик, скоро эта задача может решаться без него» — первая строка говорит о смене возможности читателя, а не о характеристиках продукта
X-09: да — весь пост держится на одной мысли (можно собрать простой продукт без разработчика), второго сюжета нет
X-11: да — рассуждение: конструкции чередуются (условие → декларатив с двоеточием → короткое предложение → список → мнение → короткая констатация → условие-императив), трёх одинаковых подряд нет
X-18: да — рассуждение: в списке из трёх пунктов (оплата, тариф, деплой) каждый пункт даёт новый факт, третий пункт про серверы не дублирует первые два

## Пост 2. TeamStream MCP (МТС Линк)
T-01: да — «читатель проверит, накопился ли у компании архив видеозаписей встреч, и попробует найти в нём нужный кусок через ИИ вместо пересмотра целиком» — судейское: конкретное действие названо, аудитория узкая (клиенты МТС Линк), но по T-12 узость не отказ
X-02: да — «Если в компании накопились десятки записей встреч, а нужный момент вы каждый раз ищете пересматриванием, для этого уже есть отдельный инструмент» — говорит о боли и её снятии, а не о том, что за протокол вышел
X-09: да — одна мысль (архив встреч можно превратить в базу знаний через ИИ), второго сюжета нет
X-11: да — рассуждение: условие, декларатив с двоеточием, декларатив с сравнением, список из четырёх пунктов, мнение, короткая оговорка, условие-императив — подряд одинаковых конструкций нет
X-18: да — рассуждение: список из четырёх сценариев, все содержательно разные (поиск, тезисы, синтез, честный отказ), лишнего третьего элемента нет

## Пост 3. Grok 4.6: цена против результата
T-01: да — «читатель добавит вторую, более дешёвую модель в свой набор инструментов вместо одной подписки» — судейское: конкретное действие названо (сравнить сервис на своей задаче)
X-02: да — «Если вы платите за одну подписку на нейросеть просто по привычке, стоит взглянуть на цены у конкурентов» — про смену привычки читателя, а не про запуск модели
X-09: да — одна мысль (не держаться одной подписки, сравнивать цену и качество), цифры API и SuperGrok поддерживают её, а не открывают второй сюжет
X-11: да — рассуждение: после правки (SuperGrok-предложение разбито на два коротких) подряд одинаковых по длине и конструкции предложений не осталось, декларативы чередуются с short/comparison-конструкциями
X-18: да — рассуждение: перечислены три отдельных факта о ценах (API, подписка, Lite-ловушка), но это не однородный ряд «третий лишний», а три разных предостережения, каждое нужно для полной картины

## Пост 4. Workspace Studio: автоматизация без кода
T-01: да — «читатель настроит в Workspace Studio один сценарий: например, автоответ в Chat или копирование шаблона файла без участия разработчика» — судейское: конкретное действие названо
X-02: да — «В Google Workspace теперь можно настроить простую автоматизацию без единой строчки кода» — про новую возможность читателя, а не про то, что Google выпустил
X-09: да — одна мысль (маленькая автоматизация без кода), список примеров её иллюстрирует, второго сюжета нет
X-11: да — рассуждение: после правки два предложения про тарифы и оплату разбиты на короткие, подряд трёх похожих длинных сложноподчинённых предложений больше нет
X-18: да — рассуждение: в списке из четырёх примеров (шаблон, папки, письмо, чат) каждый — отдельный сценарий использования, лишнего дублирующего пункта нет

## Пост 5. Zendesk в ChatGPT
T-01: да — «читатель, который ведёт поддержку в Zendesk, подключит свой аккаунт к ChatGPT и попробует получить черновик ответа на тикет из истории обращений клиента» — судейское: действие конкретное и выполнимое за один присест
X-02: да — «Если ваша служба поддержки живёт в Zendesk, у неё теперь может появиться помощник прямо внутри ChatGPT» — про то, что меняется у читателя, а не про релиз плагина
X-09: нет → ИСПРАВЛЕНО, из текста убран отдельный абзац про плагин OneNote: это был второй сюжет, который тянул на отдельный пост; факт остался в pipeline/verified.md, в посте — только Zendesk
X-11: да — рассуждение: условие, декларатив с двоеточием, мнение, декларатив с двоеточием, условие-императив — подряд одинаковых конструкций нет
X-18: да — рассуждение: третьего однородного элемента-пустышки в посте нет, весь текст об одном плагине без списка из трёх пунктов

## Пост 6. Однообразие ИИ-фото блюд
T-01: да — «читатель, который заказывает ИИ-фото блюд или товаров для меню и карточек, сравнит их с реакцией живых клиентов и обычной съёмкой, прежде чем публиковать» — судейское: действие конкретное, проверяемое
X-02: да — «Фото блюд, сгенерированные нейросетью для меню, чаще отталкивают клиентов, чем привлекают, и на это есть техническая причина» — говорит о последствии для бизнеса, а не о том, что вышла статья TechCrunch
X-09: да — одна мысль (ИИ-фото еды часто отталкивают клиентов), три причины в тексте объясняют её, а не открывают отдельные истории
X-11: да — рассуждение: подряд идут предложение-констатация, декларатив с «и», декларатив про специалиста, декларатив с двоеточием «причина», декларатив с двоеточием «проблема» — по конструкции похожи только два подряд («причина»/«проблема»), не три
X-18: да — рассуждение: три причины (усреднение по обучающим данным, эффект зловещей долины, деградация при повторной генерации) — три разных механизма, третий не повторяет первые два

## Пост 7. Legora сверяет отчётность через GPT-6 Astra
T-01: да — «читатель-бухгалтер или финдиректор попробует прогнать черновик отчётности через ИИ на сверку цифр перед подписанием, оставив финальное решение за собой» — судейское: действие конкретное, применимо к бизнесу вне AI-ниши
X-02: да — «Перед тем как подписать годовой отчёт, его сверку с исходными цифрами теперь может взять на себя ИИ» — про то, что меняется в процессе читателя, а не про выход модели GPT-6 Astra
X-09: да — одна мысль (ИИ берёт на себя рутинную сверку цифр перед подписью), кейс Legora и цифры её подтверждают, второго сюжета нет
X-11: да — рассуждение: после правки заключительных абзацев подряд одинаковых по конструкции предложений не осталось, декларативы про цифры чередуются с рассуждением и финальным императивом
X-18: да — рассуждение: в тексте нет третьего однородного элемента-пустышки, каждая цифра (41 документ, 40%, £500 000) отдельный факт из первоисточника
