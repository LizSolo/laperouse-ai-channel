# Агент(ы) для Telegram-канала LaperouseAI

## Задача (из обсуждения)

Настроить агента/агентов, которые:
- собирают новости из первоисточников (Anthropic и, возможно, другие AI-лабы)
- пишут краткий пересказ + личную оценку/комментарий к материалу
- постят это в Telegram-канал/группу

## Черновой набросок архитектуры

1. **Источники** — RSS/Atom-ленты блогов (у Anthropic, OpenAI и т.д. часто есть фиды), для остальных — периодический просмотр страницы через web-fetch.
2. **Планировщик** — задача по расписанию (cron-агент / routine), проверяет источники на новое, например раз в несколько часов.
3. **Дедупликация** — простое хранилище (файл или маленькая база) со списком уже опубликованного, чтобы не постить повторно.
4. **Обработка** — агент читает новый материал, пишет краткий пересказ и свою оценку в нужном тоне.
5. **Публикация** — пост в Telegram через Bot API (нужен токен бота и права на постинг в канал/группу).

## Ответы, полученные 2026-07-28

- **Бот и группа уже есть.** Тестовые посты сначала кидаем в группу, смотрим как выглядит, потом решаем по каналу. Название бота/токен — пришлёт отдельно.
- **Источники — шире, не только AI-лабы.** Нужны в целом последние новости про AI-технологии (не только Anthropic/OpenAI и т.п.), т.е. нужен более широкий список источников (тех-медиа, где выходят свежие AI-новости), не только один сайт (упоминался TechCrunch как пример, но одного источника мало).
- **Угол подачи / личная оценка:** каждую новость оценивать с точки зрения пользы для малого и среднего бизнеса, в любых нишах — кроме самих AI-компаний. Примеры того, что малый бизнес мог бы закрыть с помощью AI: свой сайт/приложение, бухгалтерия, юристы/договорной отдел (контракты), продажники, служба поддержки — в общем, обычные функции, которыми пользуется малый бизнес.
- **Тон текста:** не должно быть заметно, что текст написан ИИ — живой, человеческий стиль изложения.
- **Частота/расписание:** сводка каждый день в 9:00 по Таиланду.
- **Рабочий процесс:** сводка готовится → пользователь смотрит и решает, что постить и нужна ли правка, перед публикацией (человек в контуре, не полностью автономная публикация).

## Технические данные (2026-07-28)

- Бот: `@Laperouse_ask_bot` (токен и ID — в `.env`, не в этом файле).
- Тестовая группа: «Контент для Laperouse канала» (переименована 2026-07-28, была «Обращения - Laperouse AI»), supergroup, бот добавлен админом. Тестовый пост туда прошёл успешно. ID группы — в `.env` (`TELEGRAM_TEST_GROUP_ID`) и в GitHub Secrets (`TELEGRAM_CHAT_ID`), в этом файле его нет: репозиторий публичный.
- Личный чат пользователя с ботом — это НЕ группа, не перепутать при отправке. ID — в `.env` (`TELEGRAM_PERSONAL_CHAT_ID`).
- ВАЖНО: у бота ранее был webhook на `livegram.io` (сторонний конструктор ботов) — пользователь сама его отключила 2026-07-28, чтобы освободить бота под этот проект. Webhook сейчас пустой, работаем через прямые вызовы Bot API (`sendMessage` и т.п.), можно использовать `getUpdates`.
- Публикация проверена напрямую через Telegram Bot API (curl). На Windows/git-bash важно слать текст через `--data-urlencode "text@<файл>"` с текстом в UTF-8-файле (Windows-путь через `cygpath -w`) — иначе кириллица и эмодзи бьются в мусор при прямой передаче строки в `-d`.

- **Куда шлём сводку и что дальше:** дайджест каждый день в 9:00 по Таиланду уходит в тестовую группу. Пользователь сама в группе решает, что достойно публикации, что нет. ID канала для финальной публикации даст позже — только когда стиль и тон полностью её устроят после прогона тестов в группе. До этого канала в проекте нет и постить туда некуда.

- **Стиль постов утверждён 2026-07-28** — полное описание в `STYLE.md` (голос практика, спокойный тон, один текст = одна мысль, конкретика, без "магии" ИИ, точечные эмодзи, чёрный список клише и AI-паттернов типа тире-связок, "не X, а Y", риторических вопросов, итоговых саммари).

## Правки после первого пробного поста (2026-07-28)

Первый тест: пост про запуск программы OpenAI "ChatGPT for small business" (интеграции с Shopify/Intuit/Wix). Замечания:

- **Поиск новостей должен быть широким, не по узким SMB-запросам.** Не искать сразу что-то типа "AI news for small business" — так теряются источники вроде Anthropic, попадает только то, что уже само себя маркирует под SMB (в первом тесте вышел перекос в сторону OpenAI/ChatGPT). Правильный процесс: смотреть **все** свежие AI-новости (лабы, продукты, релизы — включая Anthropic и остальных), и уже из этого потока самой отбирать и оценивать, что полезно для малого/среднего бизнеса. Фильтр по SMB-пользе — это шаг обработки/оценки, а не поисковый запрос.
- **Упоминание Claude в постах — только когда реально в тему.** Если новость про фичу конкурента (например, у ChatGPT появилась прямая интеграция с Shopify/Intuit), и то же самое можно сделать через Claude (пусть не нативной интеграцией, а обходным путём — коннектором, автоматизацией и т.п.), это стоит отметить. Но не притягивать Claude искусственно в каждый пост — только когда есть реальный релевантный способ достичь того же результата.

## Список источников утверждён (2026-07-28)

Пользователь оставила все уровни, отдельно попросила поднять vc.ru и Habr в приоритет.

- **Приоритет (проверять в первую очередь):**
  - vc.ru (AI/стартапы) — здесь чаще всего всплывают интеграции AI с российскими бизнес-инструментами
  - Habr (AI-хаб)
- **Уровень 1 — блоги лабов напрямую (RSS/страница, дёшево проверять):**
  - Anthropic — anthropic.com/news
  - OpenAI — openai.com/news
  - Google DeepMind blog
  - Meta AI blog
- **Уровень 2 — общие тех-медиа про AI (через поиск/агрегацию, не читать целиком):**
  - TechCrunch (AI-раздел), VentureBeat AI, The Verge (AI-тег), Ars Technica
- **Уровень 3 — заточенные под бизнес-применение:**
  - Axios AI+, MIT Technology Review
- **Уровень 4 — русскоязычные/локальный рынок:**
  - Яндекс — не отдельный источник, а поисковый слой для нахождения русскоязычных интеграций/кейсов + Яндекс.Новости по AI-тематике

Отдельный угол для отслеживания: интеграции AI-инструментов с российским малым бизнесом (бухгалтерия, CRM и т.п.) — специально искать, не только ждать, что попадётся само.

Процесс по объёму: ежедневно один широкий поиск покрывает уровни 2-4 разом; уровень 1 (блоги лабов) проверять отдельно на новые посты, чтобы не пропустить релизы — не сильно раздувает обработку.

## Автоматическая сводка настроена (2026-07-28)

- Cloud-routine (RemoteTrigger/schedule) создан: id `trig_01UGDSW9ky7xNk8SMJbnoEUS`, https://claude.ai/code/routines/trig_01UGDSW9ky7xNk8SMJbnoEUS
- Расписание: `0 2 * * *` (UTC) = 9:00 по Бангкоку, каждый день, без выходных.
- Модель: claude-sonnet-5, окружение: Default (env_011bsggdpo8mKe73xMcmkwfp), инструменты: Bash, WebSearch, WebFetch.
- Каждый запуск: 1-2 поста, каждый отдельным сообщением в тестовую группу (id — в `.env`/Secrets), HTML-форматирование, стиль/источники/токен зашиты прямо в промпт routine (т.к. cloud-агент не видит локальные `.env`/`NOTES.md`/`STYLE.md` — облачная среда изолирована).
- Дедупликации между запусками нет (нет репозитория для хранения лога опубликованного) — принято сознательно на этапе тестов, пользователь сама отсеивает повторы при ревью. Вернуться к этому вопросу, если решим делать полноценную дедупликацию (потребует завести git-репозиторий).
- Удалить/отключить routine можно только через страницу claude.ai/code/routines — я не могу удалить программно.

## Формат взаимодействия по правкам (2026-07-28)

Уточнили: сводка приходит в Telegram-группу как превью (одностороннее — routine туда только пишет, ничего не слушает; у бота нет вебхука/поллинга). Обсуждение и правки — здесь, в этом чате с ассистентом, как весь текущий процесс. Живой чат прямо в Telegram (написала боту в группе → получила ответ там же) — отдельная, более тяжёлая задача (нужен постоянно работающий процесс, у routine-механизма минимальный интервал 1 час, для живого чата не годится). Пользователь решила: пока обсуждаем здесь, к живому чату в Telegram вернёмся позже.

## Инцидент: первый плановый запуск не дал поста (2026-07-29)

- `last_fired_at` подтвердил, что routine выполнился по расписанию 2026-07-29T02:04 UTC (09:04 Бангкок) — сработал независимо от того, был ли включён компьютер пользователя (это ожидаемо для cloud-routine, не локального процесса).
- В группу при этом ничего не пришло. Бот/токен/права в группе проверены — рабочие (getChatMember вернул admin). Значит проблема была внутри самого прогона (сам агент), не в Telegram-доступе.
- Доступа к логам/транскрипту конкретного прошедшего запуска через RemoteTrigger API нет (get/list отдают только конфиг routine, не историю запусков) — если нужно разобраться, что пошло не так, смотреть на веб-странице https://claude.ai/code/routines/trig_01UGDSW9ky7xNk8SMJbnoEUS (там должна быть история запусков и вывод).
- В 2026-07-29 ~18:00 Бангкок запустила routine вручную (`RemoteTrigger run`) как диагностику и чтобы получить сегодняшнюю сводку — session `cse_013hTAjoYqyQfghMyPEcWj1B`.
- Открыто: разобраться, почему плановый запуск не отправил пост (нужно посмотреть лог на странице routine), проверить завтрашний плановый запуск (2026-07-30 09:00 Бангкок).
- **Причина найдена (2026-07-29, из вывода ручного запуска):** облачное окружение (env_011bsggdpo8mKe73xMcmkwfp) блокирует исходящие запросы к `api.telegram.org` на уровне сетевой политики (proxy 403 на CONNECT). Это не временный сбой — намеренное ограничение окружения. Черновики постов routine написал нормально (Claude Opus 5, ChatGPT for Small Business), просто не смог их отправить.
- **Требуется действие пользователя:** на claude.ai/code → Environments → «Default» разрешить `api.telegram.org` в сетевой политике. У ассистента нет инструмента, чтобы поменять это программно.
- Пока не починено: routine не может публиковать сам. Как временное решение — 2026-07-29 ассистент вручную написал 3 поста (новые ракурсы) и опубликовал через прямой curl из локальной сессии (там сеть не ограничена): Opus 5 (цена/контекст 1M), Claude for Small Business (май, коннекторы HubSpot/QuickBooks/PayPal/DocuSign — раньше и релевантнее, чем ChatGPT-программа OpenAI), Opus 5 effort levels (переключатель усилий low/medium/high/xhigh/max, экономия токенов).
- **Обходные пути проверены и отброшены:** UI-настройки сетевой политики для Environment в вебке не нашли (прошли Routines → Dispatch — не то и не грузится; Settings → Claude Code — нет такой опции; похоже, в текущем Research Preview это просто не выведено в интерфейс). Pre-built MCP-коннектора для Telegram в каталоге Connectors нет — только форма для подключения своего готового MCP-сервера (сами не поднимали, отдельная разработка). Повторная диагностика (2026-07-29) подтвердила блок стабильно (403, retry не помогает) на втором независимом прогоне.
- **Решение (2026-07-29):** промпт routine обновлён — шаг публикации через curl убран, вместо этого агент выводит оба поста текстом прямо в финальном ответе (подписанные "Пост 1"/"Пост 2" с указанием источника). Пользователь копирует этот ответ и приносит сюда, ассистент публикует вручную (как делали весь день). Плюс добавлен пункт "не повторять темы, которые очевидно уже везде" — чтобы меньше пересекалось с тем, что мы уже тестировали руками.
- **Замечание пользователя (2026-07-29):** все 3 поста за сегодня оказались про Anthropic, хотя договаривались про 4-5 уровней источников (vc.ru/Habr в приоритете, другие лабы, тех-медиа, бизнес-медиа). Промпт routine дополнен явным правилом ротации: если последние посты были про Anthropic/Claude, следующий прогон обязан сначала проверить vc.ru, Habr и другие лабы (OpenAI, Google DeepMind, Meta) и отдавать им приоритет при выборе темы. Цель — разнообразие источников в течение недели, не Anthropic каждый день.
- **Аудит всех 12 источников (2026-07-29):** RemoteTrigger `run` с переопределённым телом на деле не переопределяет промпт — оба раза просто выполнялся обычный сохранённый routine (полезный побочный эффект: подтвердил, что правило ротации работает — второй раз выбрал OpenAI + Meta, без Anthropic). Аудит источников сделан вручную через WebSearch в этой сессии: **все 12 источников живые и дают материал за последние 1-2 дня** (vc.ru, Habr, Anthropic, OpenAI, Google DeepMind, Meta, TechCrunch, VentureBeat, The Verge, Ars Technica, MIT Tech Review — все ок), кроме Axios AI+ — это email-рассылка без обычной индексируемой ленты, плохо ищется. **Axios AI+ заменён на Inc.com** (раздел inc.com/artificial-intelligence, обычная лента + прицельно про AI для владельцев малого бизнеса) в списке источников уровня 4. Промпт routine обновлён.
- **Фактчекинг (2026-07-29):** в черновике от routine нашли похожую на выдумку деталь — несуществующий продукт «API Monster» для интеграции 1С. Проверили поиском: не существует под этим названием, реальные варианты — 1c-mcp-toolkit (GitHub) и сервис ARQA. Пост поправлен вручную перед публикацией. В промпт routine добавлено явное правило проверки фактов: не включать в пост названия конкретных продуктов/инструментов, которые не встречались напрямую в источнике (не додумывать "по логике"), особенно для нишевых коннекторов — проверять поиском, что они существуют под этим именем.
- Опубликованы ещё 2 поста (после фактчекинга и правки): ChatGPT for Small Business (OpenAI, с исправленным 1С-абзацем) и Meta Muse Spark 1.1 (агентный AI-ассистент с доступом к календарю/почте — подтверждено реальным анонсом Meta от 25 июля).
- По просьбе пользователя оформлены в посты ещё 3 находки из аудита источников: обновление спецификации MCP-протокола (28 июля, самое крупное с запуска — напрямую перекликается с темой коннекторов для 1С/CRM), FLUX 3 от Black Forest Labs (генерация видео до 20 сек со звуком из текста, для маркетинга без оператора/монтажёра), рост доли Google AI Overviews в поиске до 43% (данные Similarweb, с оговоркой про расхождение с другими замерами — важно для SEO-трафика). Все факты предварительно проверены поиском.
- Итого за 2026-07-29 в тестовой группе 8 постов на разные темы и источники (Anthropic, OpenAI, Meta, Black Forest Labs, MCP/протокольная новость, аналитика по Google-поиску).

## Философия стиля переписана (2026-07-31)

Пользователь (через советчика/партнёра) прислала развёрнутый гайд по голосу автора — акцент сместился с «инструкции редактору» на «личность автора, который пишет с нуля». `STYLE.md` полностью переписан: голос/тон/как звучит автор/как строятся тексты (короткие рубленые абзацы, буллеты, конкретный гипотетический сценарий, личное мнение прямым текстом, финал — призыв попробовать), плюс главный принцип «автор уважает время читателя, если абзац не делает текст ценнее — убери его». Строгий запрет на AI-паттерны (тире-связка, «не X, а Y», «именно», риторические клише, вывод-саммари) подтверждён и оставлен без исключений — пользователь явно подтвердила это 2026-07-31, даже когда в присланных примерах эти паттерны сами проскакивали (это артефакт быстрой правки, не разрешение). Промпт routine обновлён той же формулировкой.

**Техническое (2026-07-31):** ссылка на источник теперь прячется за словом "Источник" (`<a href="URL">Источник</a>`), картинка-превью отключается через `link_preview_options={"is_disabled":true}` в вызове sendMessage.

**Проверка работы routine:** пользователь показала результаты плановых прогонов за 2026-07-30 и 2026-07-31 (тексты приходят в самом ответе сессии на claude.ai/code, публикацию делает пользователь/ассистент вручную). Ротация источников подтверждена рабочей: 30 июля — OpenAI (ChatGPT for Small Business) + vc.ru (разбор AI-агентов в малом бизнесе), 31 июля — OpenAI (снижение цен GPT-5.6 Luna/Terra на 80%/20%) + Microsoft (программа "Copilot in 30", CSP-партнёрский триал 25 пользователей/30 дней для компаний до 300 сотрудников). Все факты перепроверены поиском и подтвердились точно. Опубликованы вручную в новом формате (скрытая ссылка, без превью).

## Схема из двух агентов (2026-07-31, вечер)

Обсуждали, не сделать ли четыре роли (сборщик → фактчекер → писатель → редактор). Пришли к двум агентам и вот почему.

**Проверка фактов должна идти ПОСЛЕ написания, а не до.** Единственный реальный провал (выдуманный «API Monster» для 1С) возник не из плохого источника: новость и факты в ней были настоящие. Модель выдумала деталь в момент написания, добавляя «полезную конкретику» в абзац. Проверяющий, работающий до написания, такое не поймает в принципе.

**Сборщик и автор не разделяются.** Тот, кто выбрал новость, понимает, чем она цепляет. Разделение теряет эту связь и добавляет лишнюю передачу между сессиями.

**Автор и редактор — обязательно разные сессии.** В одной сессии модель защищает то, что сама написала: она помнит свои доводы. Свежая сессия видит только текст и источник, ей нечего оправдывать. Это и есть настоящая независимость, ради которой стоит разделение.

**Редактор совмещает фактчек и стиль** — он читает тот же готовый текст, отдельный третий агент был бы лишним звеном.

**Чего схема не чинит:** выбор темы и ощущение голоса. Две копии одной модели имеют схожие представления о том, что интересно, поэтому редактор здесь будет соглашаться чаще, чем надо. Это остаётся на человеке — и лечится не агентами, а файлом `FEEDBACK.md`.

### Роли и расписание

- **`trig_01UGDSW9ky7xNk8SMJbnoEUS` — автор, 09:00 Бангкок (`0 2 * * *`).** Читает EXAMPLES.md, FEEDBACK.md, PUBLISHED.md, STYLE.md → ищет новости → проверяет факты по источникам → пишет посты только по подтверждённому → пушит `DRAFTS.md`.
- **`trig_01TqMJ4HYxu9AnyDG3fRKWWo` — редактор, 09:40 Бангкок (`40 2 * * *`).** Свежая сессия. Перепроверяет каждое утверждение по ссылкам сам, отдельно охотится за выдуманными названиями инструментов, проверяет стиль и тематику → пушит `REVIEW.md` → разбор приходит в группу отдельным сообщением.
- **Сторож (GitHub Action, 10:00 Бангкок).** Кодом проверяет, обновился ли `DRAFTS.md` сегодня. Если нет — пишет в группу. Не зависит от того, «вспомнит» ли модель.

### Файлы-ориентиры

- `EXAMPLES.md` — пары «вариант ассистента → переписанный автором» на одну новость. Главный ориентир по голосу, сильнее формальных правил. Разбор показал общий сдвиг: начинать с последствия для читателя, а не с цифры; разворачивать сценарий; списки вместо перечислений; личная позиция прямым текстом; шире самой новости (российский контекст, стратегия); совет по существу в финале.
- `FEEDBACK.md` — что зашло и почему. Автор дописывает пару слов, агент читает каждое утро. Это единственное, что чинит выбор темы.
- `pipeline/verified.md` — **на практике не создаётся, от него решено отказаться (2026-08-01).** Задумывался как рабочий артефакт автора и вход для редактора, но автор его ни разу не запушил, и редактор в разборе за 31 июля прямо это отметил, проверив факты самостоятельно по независимым источникам. Так даже лучше: независимая перепроверка сильнее, чем чтение чужого списка «уже проверенного». При следующей правке промпта автора убрать оттуда требование писать этот файл.

## РЕШЕНО: независимая публикация работает (2026-07-31, вечер)

Раньше считали блокировку непреодолимой. Ошибка была в диагностике: две ошибки 403 выглядели одинаково, но были разной природы.
- **Telegram:** «proxy вернул 403 на CONNECT» — сетевая политика окружения, хост недостижим. Это правда непреодолимо изнутри routine.
- **git push:** обычный «403 Forbidden» от GitHub, при том что `git clone` в том же окружении работал. То есть сеть до github.com есть, не хватало только прав (токена). Вывод «архитектурно read-only» был поспешным.

**Рабочая схема (без ноутбука, без новых расходов):**
1. Routine в 9:00 ищет новости, пишет посты, полностью перезаписывает `DRAFTS.md` и делает `git push` с fine-grained PAT (токен зашит в промпт routine).
2. Push запускает GitHub Action `.github/workflows/publish-drafts.yml`, которая вызывает `.github/scripts/send_drafts.py`.
3. Скрипт достаёт из `DRAFTS.md` все блоки в тройных обратных кавычках и отправляет содержимое каждого отдельным сообщением в тестовую группу (parse_mode=HTML, превью отключено).
4. Пользователь читает готовые посты прямо в Telegram на телефоне.

Проверено сквозным прогоном 2026-07-31: routine запушила → Action отработала за 15 сек → два поста ушли в группу (message_id 49, 50).

**Почему бесплатно:** GitHub Actions для публичных репозиториев без лимитов; «думает и пишет» по-прежнему routine из подписки. Anthropic API не задействован.

**Ключевые технические детали:**
- Токен бота и chat_id лежат в GitHub Secrets репозитория (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`), не в коде.
- Fine-grained PAT: доступ только к репозиторию `laperouse-ai-channel`, права Contents + Workflows = Read and write. **Срок действия 90 дней — истекает примерно 2026-10-29, потребуется перевыпуск и замена в промпте routine.**
- Формат `DRAFTS.md` несущий: скрипт шлёт содержимое КАЖДОГО блока с тройными кавычками. Посторонние блоки в файле приведут к лишним сообщениям. Routine обязана перезаписывать файл целиком, иначе старые посты уйдут повторно.
- OAuth-токен gh CLI не может пушить workflow-файлы (нет scope `workflow`) — для изменений в `.github/workflows/` использовать PAT.

**Что изменилось в рабочем процессе:** группа теперь не «превью, которое публикует ассистент вручную», а место, куда посты приходят автоматически. Человеческая вычитка фактов из цикла ушла — вместо неё в промпте усилено требование подтверждать каждый факт двумя независимыми источниками. Канал (когда появится) остаётся ручным: туда публикуется только то, что пользователь одобрила.

## Независимая публикация (первоначальный разбор, 2026-07-31, днём — устарело)

Публикация сейчас привязана к этой локальной сессии (у неё есть рабочий доступ к api.telegram.org, у облачного routine — нет). Разобрали план полностью независимого решения: GitHub Actions по расписанию, вызывающий Anthropic API напрямую (с веб-поиском) для исследования/написания постов и публикующий в Telegram самостоятельно, без привязки к ноутбуку. Требует отдельного платного API-ключа Anthropic (не входит в обычную подписку) и теряет ручную фактчек-проверку перед публикацией (её делает только сам промпт).

**Решение пользователя:** не платить за это сейчас — канал почти без аудитории, рано вкладываться в отдельную инфраструктуру. Остаёмся на текущей схеме: routine каждое утро готовит черновики (можно смотреть с телефона через claude.ai/code → Routines), реальная публикация — здесь, в этом чате, когда пользователь за ноутбуком. Вернуться к этому вопросу, когда будет смысл (больше аудитории/канал активен).

## Дедупликация через репозиторий подключена (2026-07-31)

> ⚠️ **Первый абзац ниже содержит ошибочный вывод и оставлен только как история.**
> Вывод «routine умеет только читать репозиторий» неверен: она нормально пушит с fine-grained PAT,
> на этом держится вся текущая публикация — см. раздел «РЕШЕНО: независимая публикация работает».
> Второй абзац (как реально работает дедупликация через `PUBLISHED.md`) — актуален.

Создан приватный → потом публичный (нужно было для доступа облачного routine без лишней возни с GitHub App) репозиторий https://github.com/LizSolo/laperouse-ai-channel с NOTES.md/STYLE.md/PUBLISHED.md. Routine подключён к нему как `sources: git_repository` — тогда решили, что это **доступ только на чтение**, потому что `git push` из облачного окружения стабильно падал с 403, даже после настройки GitHub-подключения к аккаунту claude.ai (позже выяснилось, что дело было в отсутствии токена, а не в механизме). Похоже, `sources: git_repository` в CCR — это односторонний механизм для контекста, не полноценный git remote. Пробовали дать routine писать черновики в `DRAFTS.md` и пушить самой (чтобы пользователь могла читать их с телефона через GitHub, а не через claude.ai/code) — не вышло по той же причине. Промпт возвращён к дублированию черновиков текстом в ответе сессии.

**Как реально работает дедупликация сейчас:** `PUBLISHED.md` в репозитории содержит только темы, которые пользователь лично одобрила/переписала как финальные (не все черновики, что уходят в тестовую группу). Routine читает этот файл в начале каждого прогона и не предлагает те же темы повторно.

## Источники: YouTube добавлен (2026-07-31)

Обсуждали видео-источники (YouTube/Instagram/Facebook/LinkedIn). Instagram/Facebook/LinkedIn — не годятся: официальные API только для верифицированных бизнес-партнёров, скрейпинг нарушает ToS с реальным юридическим риском (LinkedIn судится с нарушителями). YouTube — добавлен как источник в промпт routine через обычный WebSearch (без выделенного MCP-инструмента — готовые YouTube MCP-серверы типа `wynandw87/claude-code-youtube-mcp` работают только локально через stdio, недоступны облачному routine).

## WebFetch полностью заблокирован в облачном окружении (2026-08-01)

При плановом прогоне автора обнаружено: `WebFetch` и прямой `curl` из этой сессии получают 403 на CONNECT абсолютно для всех хостов без исключения, включая нейтральные (example.com, google.com) и даже anthropic.com, который в `noProxy` списке. `curl -sS "$HTTPS_PROXY/__agentproxy/status"` подтверждает через `recentRelayFailures`: `"gateway answered 403 to CONNECT (policy denial or upstream failure)"` — это не сбой конкретного сайта, а сетевая политика окружения (см. `/root/.ccr/README.md`, раздел "403/407 from the proxy"). `WebSearch` при этом работает нормально и отдаёт содержательные результаты.

Это ломает шаг фактчекинга из промпта автора буквально ("открой первоисточник через WebFetch, подтверди вторым источником"), потому что WebFetch недоступен целиком, не только для конкретных заблокированных доменов, как было с api.telegram.org раньше. Обошли так: собрали факты через WebSearch по 8+ независимым публикациям одной новости (включая официальный пресс-релиз компании через BusinessWire) — цифры совпали дословно везде, расхождений не нашли. Это слабее прямого чтения первоисточника, но на практике сопоставимо по надёжности, когда независимых публикаций много и они совпадают.

**Не чинили и не пытались обойти** (см. README: "do not retry or route around it — report the blocked host"). Не знаем, разовый ли это сбой окружения или постоянная политика конкретного `environment_id` — если повторится завтра, это подтвердит постоянство, аналогично истории с блокировкой api.telegram.org.

**Открытый вопрос:** возможно, стоит переформулировать шаг фактчекинга в промпте автора так, чтобы он не требовал буквально WebFetch, а допускал перекрёстную проверку через WebSearch как равноценный метод, раз WebFetch недоступен на уровне окружения. Пока не менялось — решение за пользователем.

**Закрыто 2026-08-05:** формулировка в промпт добавлена. Практика показала, что агент четыре дня подряд справлялся сам, подменяя WebFetch перекрёстным WebSearch, и посты выходили фактически чистыми. Причиной сбоя 05.08 это не было.

## Инцидент: облачная сессия не смогла запушить в GitHub (2026-08-05)

Утренний прогон не дал ни постов, ни разбора. Разобрали по шагам:

- Обе routine **запускались** (автор 02:03 UTC, редактор 02:40 UTC), `enabled: true`, `suspension_reason` пустой. Дело не в расписании.
- Коммитов за 05.08 в репозитории нет вообще, ни в master, ни в других ветках.
- Редактор промолчал **правильно**: в его промпте есть инструкция не писать `REVIEW.md`, если дата в `DRAFTS.md` не сегодняшняя. Сбой один, а не два.
- Сторож отработал и доставил уведомление в Telegram. Бот, chat_id и оба Action исправны.
- **PAT жив**: проверен напрямую через api.github.com, `HTTP 200`, права на репозиторий `push` и `admin`. Дело не в токене и не в сроке его действия.

Вывод: в облачной сессии заблокирован сам `git push` к github.com — тот же класс отказа, что с `api.telegram.org` и WebFetch. Клон в сессию приезжает средствами платформы, поэтому чтение репозитория работает, а запись уходит в сеть и упирается в прокси.

Агент, упёршись в блок, попробовал обойти его через GitHub MCP и получил другую ошибку: «GitHub access is not enabled for this session. An org admin must connect the Claude GitHub App for this organization». **К диагнозу это отношения не имеет** (конвейер никогда не ходил через GitHub App), но как вариант починки годится: подключение Claude GitHub App даёт легальный канал записи мимо прокси.

Сегодняшний пост опубликован вручную с ноутбука. Архитектуру не трогали: 1–4 августа push проходил, 5 августа нет, одной точки данных мало. **План:** прогон 06.08 оставлен нетронутым как чистый тест. Повторится — подключаем Claude GitHub App, и только если и это не сработает, обсуждаем перенос конвейера в сам GitHub Actions.

## Разбор шести опубликованных постов, промпт автора переписан (2026-08-05)

Лиза прислала всё, что реально вышло в канал за 30.07–05.08. Сопоставили с черновиками агента из истории `DRAFTS.md`.

Счёт: агент выдал восемь постов, в канал ушли два, **и ни один не опубликован без переписывания**. Ещё два поста Лиза нашла сама, и оба не новости. Главная проблема агента оказалась не в тексте, а в выборе темы.

Что зафиксировано в файлах:
- `EXAMPLES.md` — пары 3 (shadow AI) и 4 (ИИ-ресепшионист) с разбором «что изменилось». Пара 4 самая показательная: агент написал про Zoom и дал российские аналоги строкой в конце, Лиза перевернула — Zoom на два предложения, тело поста про Mango Office, МТС Бизнес и МегаФон.
- `PUBLISHED.md` — четыре новые темы.
- `FEEDBACK.md` — отклонённые темы с причинами и выведенные закономерности.

Что изменено в промпте автора (routine `trig_01UGDSW9ky7xNk8SMJbnoEUS`):
- Снято жёсткое требование свежести за 24–48 часов. **Уточнено в тот же день:** плоского «свежесть необязательна» оказалось мало, агент мог с равным правом взять вчерашнюю новость и статью месячной давности. Лиза попросила явную очерёдность, и теперь в ШАГ 2 стоит порядок: сначала сутки-двое, потом неделя, и только потом несвежее. Плюс требование не выдавать старое за новость: либо честно обозначить давность первой строкой (как в посте от 04.08), либо писать как объяснение вне времени (как пост про Effort от 03.08). Согласовано с `FEEDBACK.md` и `EXAMPLES.md`.
- Российские инструменты подняты на первое место в списке источников, добавлена справка и документация инструментов как источник тем.
- Добавлен блок НЕ БРАТЬ: раунды инвестиций и оценки компаний, анонсы недоступных продуктов, безопасность ИИ-лабораторий, западные сервисы, которые нельзя подключить из России. **Правка в тот же день:** пункт про анонсы Лиза убрала, темы вида «выйдет до конца сентября» снова допустимы. Соответствующее обобщение вычищено и из `FEEDBACK.md`, чтобы фильтр по темам не воспроизводил снятый запрет.
- Добавлено правило «западная новость как повод»: свернуть до двух предложений, телом поста сделать российские инструменты с разбором порога входа и стоимости внедрения.
- Добавлено «лучше сократить, чем добавить» и один развёрнутый сценарий на пост вместо двух.
- **Снят лимит на число постов (05.08, по просьбе Лизы).** Было «1-2 поста», стало «сколько тем прошло отбор и проверку». Логика: в группу уходят кандидаты, Лиза сама выбирает, что достойно канала, поэтому выбор ей полезнее лимита. Заодно из ШАГ 2 убрано «нашёл тему — дальше не ищи», иначе агент останавливался бы на первой. Взамен усилена защита от халтуры: проверка фактов на каждую тему полная, непроверенная тема в пост не идёт, три проверенных поста лучше шести наспех. Старое описание «1-2 поста» осталось в записи от 28.07 как исторический факт.
- Добавлена проверка цен: помесячная и годовая оплата это разные числа, годовую не вычислять самостоятельно (реальная ошибка в посте от 05.08).
- Уточнён заголовок первой строкой жирным (агент 05.08 выдал пост вообще без HTML-разметки).

Шаг публикации намеренно не тронут, чтобы прогон 06.08 остался чистым тестом на блокировку GitHub.

## Выпуск 05.08 собран вручную, и что это показало про поиск тем

После того как утренний прогон не дошёл до репозитория, посты за 05.08 собраны вручную по новым правилам. Ушло два: ИИ-найм в СберБизнесе (`message_id=72`) и конец бесплатного периода у надстройки ChatGPT для PowerPoint (`message_id=73`).

**Главный урок не про блокировку, а про поиск.** Первый заход дал ровно одну тему, и вывод «больше ничего подходящего нет» оказался неверным. Причина: все запросы были в жанре новостной ленты, «кто что запустил вчера». Это покрывает только категории источников 3-5. Категорию 2 (справка и документация инструментов, которыми читатель уже пользуется) не открывали вообще, хотя она стоит вторым приоритетом.

Во втором заходе именно оттуда пришла самая ценная тема: у надстройки ChatGPT для PowerPoint бесплатный период заканчивался на следующий день. Дедлайн на завтра в новостную ленту не попадает никогда, он лежит в release notes.

В `prompts/author.md` добавлен обязательный минимум: прежде чем объявить, что тем нет, обойти не меньше трёх категорий, и категория 2 входит всегда. Отдельно записано, что YouTube на август 2026 отдаёт одни подборки промптов и время на него тратить не стоит.

**Отдельно про качество источников.** По теме PowerPoint единственная публикация с цифрами расхода кредитов (`windowsnews.ai`) приводила числа, которых нет больше нигде, и смешивала кредиты OpenAI с кредитами Microsoft 365. Цифры в пост не пошли. Это ровно тот случай, ради которого написано правило «расхождение между источниками — повод не писать цифру вовсе».

## Промпты агентов переехали в репозиторий (2026-08-05)

До сих пор промпты автора и редактора существовали только в настройках routine на claude.ai/code. Файла, который можно открыть и почитать, не было ни одного, при том что это самые важные инструкции в проекте. Правила стиля жили в двух местах сразу (в `STYLE.md` и внутри промптов) и могли молча разъехаться.

Теперь инструкции лежат в `prompts/author.md` и `prompts/editor.md`.

**Синхронизации нет, потому что копия одна.** Промпт в routine сведён к тонкому загрузчику: «репозиторий клонирован, прочитай `prompts/author.md` и работай ровно по нему; если файл расходится с этим сообщением, прав файл». Репозиторий клонируется в сессию каждое утро, поэтому правка в файле начинает действовать со следующего прогона сама. Отдельно обновлять что-то на claude.ai/code больше не нужно.

**Что осталось в routine.** Только блок публикации: `git config`, `git add`, `commit` и `push` с настоящим PAT. В публичный файл он поехать не может, поэтому шаг публикации в `prompts/*.md` ссылается на системный промпт, а не повторяет команды. Заглушки `<PAT>` в файлах больше нет, ссылаться не на что.

**Защита от пустого файла.** Если `prompts/*.md` не найден или пуст, агент обязан ничего не публиковать, сообщить об этом в финальном ответе и завершить работу. Без этого он попробовал бы восстановить инструкцию по памяти и выдал бы пост мимо всех правил.

**Что это меняет на практике.** Правило стиля теперь живёт в одном месте. Раньше `STYLE.md` и промпт были двумя копиями, которые расходились молча, и `CLAUDE.md` предупреждал об этом отдельным пунктом.

**Риск.** Прогон стал зависеть от одного файла в репозитории. Сломанный или случайно удалённый `prompts/author.md` останавливает выпуск целиком. Сторож это поймает в 11:00, потому что `DRAFTS.md` не обновится.

**Про запрещённые обороты.** На шести постах видно, что «именно», «не X, а Y» и «поэтому» перед выводом — не следы быстрой правки, как записали 31.07, а обычная речь Лизы: обороты есть в пяти постах из шести. Прежнее объяснение в `EXAMPLES.md` заменено. Сам запрет для агента остался строгим: в живом авторском тексте эти конструкции звучат нормально, в машинном читаются как след модели, поэтому ограничение на агента жёстче, чем на автора.

## Правила переписаны в проверяемый формат, добавлена самопроверка (2026-08-05)

Претензия автора канала: инструкции написаны как эссе для человека, агент должен вычитывать смысл между строк. Формулировки вроде «спокойная уверенность без желания что-либо доказать» и «воздуха больше» проверить нельзя, ответить по ним да/нет невозможно.

**Диагноз оказался неполным.** Расплывчатость реальна, но главная поломка была в другом: у стиля не было ворот. У фактов ворота стояли с самого начала — ШАГ 3, `verified.md`, запрет писать то, чего в файле нет, — и факты действительно выправились, редактор поймал подставленную годовую цену. Текст же писался и пушился без единой сверки с правилами. Любой чек-лист без обязательного прохода перед push остался бы украшением.

**Что сделано.**

- `RULES.md` — единственный файл проверяемых правил, пять блоков с ID: ТЕМА (T), ФАКТЫ (F), ТЕКСТ (X), ФОРМАТ (M), ЗАПРЕТЫ (Z). Каждое правило формулируется так, чтобы ответ был да/нет.
- ШАГ 4.5 в `prompts/author.md`: перед push агент проходит все ID и пишет `pipeline/selfcheck.md` строками `ID: да|нет — цитата`. Непочиненное «нет» блокирует публикацию поста.
- Редактор в ШАГЕ 3 проверяет текст по тем же ID сам и сверяет с `selfcheck.md`. Расхождение (проставлено «да» там, где правило нарушено) объявлено находкой второй важности после неподтверждённого факта. Это защита от формальной самопроверки: агенту незачем врать себе, если через сорок минут придёт независимая сессия с тем же списком.
- Дублирование убрано. Запрет на AI-паттерны лежал в четырёх файлах разными словами (`STYLE.md`, `prompts/author.md`, `EXAMPLES.md`, `CLAUDE.md`), правила подачи — в трёх. Модель не складывает три версии в одну, она выбирает какую-то одну. Теперь проверяемое живёт только в `RULES.md`, `STYLE.md` остался голосом, промпты ссылаются на ID.

**Пороги измерены, а не назначены.** X-08 (длина 1500–2800 знаков) и X-04 (абзац ≤ 3 предложений) выведены из четырёх опубликованных вариантов автора в `EXAMPLES.md`: 1659, 2350, 2491 и 2701 знак, абзацы типично в 1–2 предложения. Первая версия правила «в первом предложении нет названия компании» проверку эталонами не прошла: два поста из четырёх открываются словами «Новый Claude Opus 5…» и «Google всё чаще отвечает…». Правило переписано в X-01: в первом абзаце нет чисел. Это выполняется во всех четырёх.

**Длина: 1800 знаков потолок, и это строже, чем пишет сама автор.** Первая версия X-08 брала диапазон из эталонов, 1500–2800. Автор канала в тот же день сказала, что хочет короче, и что не понимает, почему у агента выходят длинные полотна. Порог опущен до 900–1800. Её собственные опубликованные посты в него не влезают (2350, 2491, 2701 знака), влезает только пара 3 — 1659, тот самый пост про shadow AI, который она сократила вдвое перед публикацией. Расхождение осознанное: правило описывает, какой пост она хочет получать, а не какой пишет сама.

**Почему выходило длинно.** Правила аддитивные: личная оценка, сценарий, список, российский контекст, финальное действие — каждое требует что-то добавить. Убавляло ровно одно, и то мягкой формулировкой «лучше сократить». Мягкая инструкция проигрывает пяти жёстким. Лечится двумя вещами: жёстким числом в X-08 и отдельным правилом X-16 с порядком удаления (второй сюжет → очевидное пояснение → вводный абзац → половина списка → второй пример). Личная оценка и финальное действие режутся последними. В `prompts/author.md` добавлено: писать в два прохода, свободный черновик и отдельный проход на сокращение. Попытка сразу писать коротко даёт сухой текст, а это ровно та претензия, с которой начинались правки 31.07.

**Расписание не сдвигали.** Проверено по коммитам: автор заканчивал в 09:09, 09:10, 09:11 и 09:12 четыре дня подряд, редактор стартует в 09:42. Запас около тридцати минут, самопроверка столько не съест. Если `DRAFTS.md` начнёт приходить после 09:30, сдвигать редактора на 10:15 и сторожа на 11:30.

**Правила формата требовали ровно того, что автор вычищает (исправлено в тот же день).** Считано по `EXAMPLES.md`: метки «Нюанс» и «Проверить просто» встречаются пять раз в вариантах ассистента и ни разу в опубликованных, а в истории `DRAFTS.md` — восемь раз. При этом M-03 предписывал их ставить. Ни один из четырёх постов автора не начинается заголовком отдельной строкой, а M-01 требовал заголовок. Оба правила переписаны: первая строка — обычная первая фраза текста, метки-ярлыки запрещены, оговорка пишется предложением. Добавлено M-02 с потолком в два жирных фрагмента на пост.

**Разметка в эталонах потеряна при копировании (выяснено 2026-08-05).** В вариантах автора в `EXAMPLES.md` нет ни одного HTML-тега, в вариантах ассистента теги есть. Автор канала подтвердила: жирный шрифт и эмодзи в её опубликованных постах есть. Значит варианты автора копировались из Telegram, где форматированный текст копируется плоским, а варианты ассистента брались из `DRAFTS.md`. В шапку `EXAMPLES.md` добавлено предупреждение: по этому файлу разметку измерить нельзя, измеряются только слова и структура.

Проверка на прочность двух правил, которые из этого наблюдения выросли: M-01 и M-03 держатся на словах, а не на тегах. Метки «Нюанс» и «Проверить просто» при копировании никуда бы не делись, их ноль. Заголовок первой строкой тоже остался бы текстом, его нет ни в одном из четырёх постов. Оба правила в силе. А вот M-02 стоял на выдуманном числе «не больше двух жирных фрагментов на пост»: измерить плотность разметки не по чему. Переписан на «не больше одного фрагмента на абзац, никогда целое предложение».

**Чего чек-лист не заменяет.** Голос задаётся примерами, а не правилами. `EXAMPLES.md` остаётся главным ориентиром, и самое полезное пополнение конвейера — не новое правило, а очередная пара «до/после».

**Рубрика про Claude.** Автор канала попросила добавить Anthropic и Claude как постоянное направление. Решено не менять общий приоритет отбора, а завести отдельную рубрику: каждый прогон агент дополнительно смотрит `anthropic.com/news`, `docs.claude.com` и release notes, и полезное изменение идёт отдельным постом сверх обычного отбора. Правило T-05 (западный сервис нельзя основной темой) на Claude и ChatGPT не распространяется: аудитория ими пользуется, пост про настройку Effort это подтвердил.

**Дыра в обратной связи.** По отклонённому посту про Яндекс от 04.08 причина не зафиксирована: тема одобрена, текст отклонён, чем именно плох — не записано. В `FEEDBACK.md` вместо причины стояла инструкция «посмотри в истории и сравни с эталонами», то есть ровно та расплывчатость, против которой всё это затевалось. Строка заменена на честное «причина не зафиксирована, выводить правила из этого случая нельзя». Туда же добавлен формат обратной связи в три пункта: тема или текст → цитата места → как надо было.

## Инцидент повторился: push всё ещё не проходит (2026-08-06)

Прогон 06.08 был оставлен как чистый тест на блокировку (см. инцидент 05.08 выше).
Результат другой, чем 05.08, и диагноз сдвинулся:

- Тема и текст собраны по всем пяти файлам контекста, факты проверены (WebFetch
  снова 403 на всех доменах, как обычно, сработал фолбэк через WebSearch), два поста
  прошли самопроверку по `RULES.md` без единого непочиненного «нет». Проблема не в
  контенте, а строго в шаге 5.
- **Прямой `git push` с PAT из системного промпта отдал HTTP 403 от самого GitHub**,
  не от прокси: `curl "$HTTPS_PROXY/__agentproxy/status"` после отказа показал
  `recentRelayFailures: []` — запрос прокси пропустил, отказ пришёл от api.github.com.
  Чтение тем же токеном (`git ls-remote`) отработало нормально, значит проблема именно
  с правом записи у этого конкретного PAT сегодня, а не с сетью и не с токеном вообще
  (05.08 диагностировали ровно обратное: сеть блокировала сам `git push`, права
  токена были подтверждены отдельной проверкой). Из одной точки данных нельзя сказать,
  протух ли конкретно этот PAT, или это снова сетевой блок, который в этот раз дал
  другой код ошибки.
- **Второй канал — GitHub MCP (Claude GitHub App) — сегодня уже подключён** (в отличие
  от 05.08, когда он давал «GitHub access is not enabled for this session»). Через него
  `get_me` и чтение файлов работают, но `push_files` отдал `403 Resource not accessible
  by integration` — у установленного приложения нет права записи (`contents: write`)
  в этот репозиторий, только чтение.
- Из плана 05.08 («повторится — подключаем GitHub App, не поможет — переносим конвейер
  в сам GitHub Actions») первый шаг фактически сделан, но не помог. Публикация была
  заблокирована на обоих доступных каналах записи.

**Не подтверждено, потому что не проверялось:** протух ли именно сегодняшний PAT
(нужна отдельная проверка правами через API вне этой сессии) и есть ли у Claude GitHub
App вообще опция выдать `contents: write` на этот репозиторий из настроек GitHub —
обе проверки нужны за пределами облачной сессии, руками пользователя.

Черновики за 06.08 лежат только локально в контейнере сессии и потеряются при её
завершении. Тексты продублированы в финальном ответе агента, публикация или подстановка
нового PAT — на пользователе.

**Обновление (2026-08-06, тем же днём): GitHub App переустановлен с правом записи.**
Пользователь подтвердила: GitHub App теперь установлен на `laperouse-ai-channel` именно
с правом записи. Повторный `push_files` (только `NOTES.md`, без `DRAFTS.md`, `REVIEW.md`
и `pipeline/`, по прямой просьбе пользователя) прошёл — вывод из инцидента 05.08
подтвердился на практике: второй канал записи через GitHub App действительно чинит
блокировку, когда установка приложения получает `contents: write`. Открытым остаётся
только состояние прямого `git push` с PAT из промпта routine — его отдельно никто не
перепроверял, чинить его смысла нет, раз GitHub App уже работает.

## Открытые вопросы (актуально на 2026-07-31)

- Канал для финальной публикации — появится позже, сейчас не создаём и не настраиваем.
- Хэштеги по темам (кроме авторского #ЛизаСоло) — отложено, вернуться позже.
- Хэштег для партнёрши (второй авторки канала) — завести, когда она определится с именем/ником.
- Живой чат с ботом прямо в Telegram — отложено на будущее (нашли готовые open-source мосты типа `daxaur/openpaw`, но они тоже требуют постоянно запущенного локального процесса — та же зависимость от ноутбука, от которой пользователь решила пока не уходить).
- Перевыпуск PAT примерно 2026-10-29, когда истечёт срок.
