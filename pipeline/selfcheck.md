# Самопроверка за 2026-08-16
Скрипт: pipeline/lint.py, код 0, отчёт в pipeline/lint.md

## Пост 1. ChatGPT Business — Computer History
T-01: да — «Если у вас есть сотрудники на Mac с доступом к ChatGPT Business, откройте вместе с ними настройки Integrations и решите, стоит ли включать Computer History» — действие названо глаголом (открыть, решить), читатель — обычный владелец бизнеса с сотрудниками на Mac, а не AI-специалист
X-02: да — первое предложение про то, что меняется для читателя (не придётся пересказывать контекст), а не про то, какой продукт вышел и с какими характеристиками
X-09: да — одна мысль: что за функция и как её включить; второго сюжета, который тянул бы на отдельный пост, нет
X-11: да — рядом нет трёх подряд предложений одинаковой длины и конструкции (проверено по всему тексту)
X-18: да — триад с лишним третьим элементом в тексте нет

## Пост 2. Anthropic — сканирование скиллов и плагинов (бета)
T-01: да — «спросите, откуда они берутся и кто их проверяет до того, как включить в работу» — действие для админа компании названо; пометка [узко: Enterprise-план] не отменяет действие, а сужает, кому оно доступно
X-02: да — первое предложение про изменение для читателя (скиллы теперь можно проверить), а не про то, что за продукт выпустила Anthropic
X-09: да — одна мысль: как работает сканирование и как его включить
X-11: да — трёх подряд предложений одинаковой конструкции нет
X-18: да — список из трёх исходов сканирования (pass/warn/fail) — каждый добавляет своё значение, лишнего элемента, который дублировал бы два других, нет

## Пост 3. Notion — шаринг контекста с Custom Agent из меню Share
T-01: да — «Откройте документ, с которым работает ваш Custom Agent в Notion, и поделитесь им через обычное меню Share вместо настроек агента» — конкретное действие названо
X-02: да — первая строка про то, что читателю больше не нужно делать (не нужно лезть в настройки агента), а не про характеристики функции
X-09: да — одна мысль про шаринг контекста из привычного меню, второго сюжета нет
X-11: да — трёх подряд предложений одинаковой длины и конструкции нет
X-18: да — триад в тексте нет

## Пост 4. Google — Gemini 3.7 Flash
T-01: да — «посчитайте стоимость текущего месяца по новой цене и сравните с тем, что платите сейчас» — действие для команд с разработкой на Gemini API; пометка [узко: разработчики] сужает аудиторию, но не отменяет действие
X-02: да — первое предложение про то, что меняется для читателя (счёт может стать вдвое меньше), а не про характеристики новой модели
X-09: да — одна мысль: цена стала ниже, что с этим делать; абзац про Gemini Spark не разворачивается во второй сюжет, а поясняет, кому экономия не так заметна
X-11: да — трёх подряд предложений одинаковой конструкции нет (два соседних предложения про доступ для разработчиков и для бизнеса построены похоже, но третье рядом с ними уже другое)
X-18: да — триад в тексте нет

## Пост 5. OpenAI — программа для малого бизнеса вокруг ChatGPT Work
T-01: да — «Возьмите одну повторяющуюся многошаговую задачу и один раз поручите её целиком ChatGPT Work вместо серии отдельных вопросов» — конкретное действие названо
X-02: да — первое предложение про смену способа работы с ChatGPT (задачу можно отдать целиком, а не командами по одной), а не про то, что запустила OpenAI
X-09: да — одна мысль про агента ChatGPT Work; остальные части программы (гайды, интеграции, очные встречи) поданы кратко списком и не разворачиваются в отдельный сюжет
X-11: да — трёх подряд предложений одинаковой длины и конструкции нет
X-18: да — список из трёх частей программы — каждая добавляет своё (гайды, интеграции, офлайн-события), лишнего элемента нет
