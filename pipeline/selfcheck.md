# Самопроверка за 2026-08-19
Скрипт: pipeline/lint.py, код 0, отчёт в pipeline/lint.md

Режим отбора включён (T-15): ниже только пять судейских правил на пост —
T-01, X-02, X-09, X-11, X-18. Остальные несчётные правила проверены при письме,
но строк по ним здесь нет: их закрывает поток постов, а не отчёт.

## Пост 1. Claude-агент вместо ручных договоров, счетов и актов
T-01: да — «владелец бизнеса заведёт в Claude свой skill-шаблон и сократит подготовку договора, счёта и акта на клиента с получаса до минуты плюс проверка» — действие названо глаголом («заведёт», «сократит»), читатель вне AI-ниши: любой предприниматель, который готовит типовые документы клиентам
X-02: да — «Договор, счёт и акт на каждого клиента обычно съедают у предпринимателя лишние полчаса ручной работы, и эту рутину можно передать агенту Claude» — первое предложение о том, что меняется для читателя (время освобождается), а не о характеристиках продукта
X-09: да — в посте одна мысль: передать сборку типового комплекта документов skill-у в Claude; второго сюжета, который тянул бы на отдельный пост, нет
X-11: да — рассуждение: абзацы чередуются по конструкции (короткое утверждение, объяснение, список, личная оценка, призыв), трёх подряд одинаковых по длине и синтаксису предложений нет
X-18: да — рассуждение: список реквизитов и список форматов файлов несут разную фактическую информацию, лишнего третьего элемента ради округления фразы нет

## Пост 2. Навык /design в Claude Code
T-01: да — «разработчик или дизайнер вызовет /design в Claude Code и получит несколько вариантов интерфейса сразу с кодом, не переключаясь между инструментами» — действие названо глаголом; аудитория узкая, названа в служебной строке пометкой [узко]
X-02: да — «Если вы разрабатываете продукт в Claude Code, макет интерфейса больше не нужно собирать в отдельном инструменте» — первое предложение о том, что меняется для читателя, а не о том, какой продукт вышел
X-09: да — одна мысль: дизайн-холст теперь работает прямо в Claude Code; второго сюжета нет
X-11: да — рассуждение: абзацы разной длины и построения (объяснение через двоеточие, перечисление через «или», список возможностей, условия доступа), три подряд одинаковых нет
X-18: да — рассуждение: перечисление плана подписки (Pro, Max, Team, Enterprise) и перечисление способов экспорта (PDF, PPTX, Figma/Adobe/Canva) фактические и не имеют лишнего третьего элемента для симметрии

## Пост 3. Блокировка спам-приглашений в Google Calendar
T-01: да — «пользователь Google Workspace включит блокировку конкретного отправителя и перестанет получать от него спам-приглашения в календаре» — действие названо глаголом, читатель вне AI-ниши
X-02: да — «Если в вашем календаре регулярно всплывают приглашения на встречи от подозрительных отправителей, с этой недели их можно блокировать насовсем» — первое предложение о последствии для читателя, не о том, что выпустил Google
X-09: да — одна мысль: новая функция блокировки отправителей в календаре; второго сюжета нет
X-11: да — рассуждение: абзацы построены по-разному (объяснение механики, ограничение, тарифы и сроки, личная оценка, призыв), трёх подряд одинаковых по длине и структуре предложений нет
X-18: да — рассуждение: в тексте нет троек однородных элементов вообще, значит и лишнего третьего элемента ради округления фразы тоже нет
